@model RolModelo
@{
    ViewData["Title"] = "Desactivar rol";
}

<h1>@ViewData["Title"]</h1>

<p class="text-muted">Hay usuarios y permisos asociados a roles. No se elimina el registro: se desactiva y se guarda la fecha de desactivación.</p>

<hr />
<dl class="row">
    <dt class="col-sm-2">Nombre</dt>
    <dd class="col-sm-10">@Model.Nombre</dd>
    <dt class="col-sm-2">Descripción</dt>
    <dd class="col-sm-10">@Model.Descripcion</dd>
    <dt class="col-sm-2">Activo</dt>
    <dd class="col-sm-10">@(Model.Activo ? "Sí" : "No")</dd>
</dl>

<form asp-action="Delete" asp-route-id="@Model.IdRol" method="post">
    <button type="submit" class="btn btn-danger">Confirmar desactivación</button>
    <a asp-action="Index" class="btn btn-outline-secondary">Cancelar</a>
</form>
